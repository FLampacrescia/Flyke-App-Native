import {
    API_URL,
    FILES_URL,
    FRONT_URL,
    // MP_PUBLIC_KEY,
    // MP_ACCESS_TOKEN,
} from '@env';

const config = {
    API_URL: API_URL,
    FILES_URL: FILES_URL,
    FRONT_URL: FRONT_URL,
    // MP_PUBLIC_KEY: MP_PUBLIC_KEY,
    // MP_ACCESS_TOKEN: MP_ACCESS_TOKEN,
};

export default config;